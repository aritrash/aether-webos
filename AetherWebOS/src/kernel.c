void kernel_main(){
    //For now, this is a placeholder.
    // Add UART driver code next to print AetherWebOS logo and version
    // info on remote screen via Serial.

    while(1){
        // CPU heartbeat loop
    }
}